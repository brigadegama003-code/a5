document.querySelector('.menu')?.addEventListener('click',()=>document.querySelector('nav')?.classList.toggle('open'));const buttons=document.querySelectorAll('[data-filter]'),cards=document.querySelectorAll('[data-cat]');buttons.forEach(b=>b.addEventListener('click',()=>{buttons.forEach(x=>x.classList.remove('active'));b.classList.add('active');cards.forEach(c=>c.style.display=b.dataset.filter==='All'||c.dataset.cat===b.dataset.filter?'block':'none')}));document.querySelector('form')?.addEventListener('submit',e=>{e.preventDefault();alert('Thank you. Connect this demo form to an email service before launch.')});